// Placeholder — diagram assets live in src/content/<category>/<topic>/diagram.png
// On web we simply return the path; native uses require() at content definition site.
export function getDiagramPath(_slug: string): null {
  return null;
}
